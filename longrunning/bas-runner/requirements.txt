# bas-runner Runtime image dependencies (pinned loosely; pin exact for a release).
# bedrock_agentcore provides the long-running Runtime app + async-task heartbeat.
bedrock-agentcore>=1.19.0
boto3>=1.43.62
# The Play-Mode / sandbox-hook logic this runner reuses lives in sentinel-harness.
# In the image, install it from the repo (built wheel or `pip install -e .` of the
# repo root); listed here so the intent is explicit.
# sentinel-harness
